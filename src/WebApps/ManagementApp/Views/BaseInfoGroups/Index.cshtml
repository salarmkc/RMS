@model IEnumerable<ApplicationCore.ViewModel.BaseInfo.BaseInfoGroupViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">افزودن</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BaseInfoGroupName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BaseInfoGroupText)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BaseInfoGroupName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BaseInfoGroupText)
            </td>
          
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">ویرایش</a> |
                <a asp-action="Details" asp-route-id="@item.Id">جزئیات</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">حذف</a>
            </td>
        </tr>
}
    </tbody>
</table>
